<odoo>
    <data>

    <record id="pos_config_user_rule" model="ir.rule">
        <field name="name">POS Config User Rule</field>
        <field name="model_id" ref="model_pos_config"/>
        <field name="global" eval="True"/>
        <field name="domain_force">[('user_ids.id','child_of',user.id)]</field>
    </record>

    </data>
</odoo>
